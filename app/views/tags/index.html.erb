<h1>All the Tags, All the Time</h1>

<ul id="tags">
	<% @tags.each do |tag| %>
		<p>
		<%= tag.name %>	
		</p>
	<% end %>
	
</ul>

<%= link_to 'New tag', new_tag_path, class: 'new_tag' %>
<%= link_to "delete", tag_path(@tag), method: :destroy, data: {confirm: "Really delete the tag?"}
 %>